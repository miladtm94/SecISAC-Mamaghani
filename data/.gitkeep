# This directory stores large data files that are NOT tracked by Git.
# Required files (download or generate separately):
#   myBuffer.mat               — pre-computed resource buffer (~GB)
#   myGroundTerminalDist.mat   — ground terminal locations
#   ObstacleLocs.mat           — obstacle geometry
#   trajectorySet.mat          — generated by Plot_Trajectories.m
